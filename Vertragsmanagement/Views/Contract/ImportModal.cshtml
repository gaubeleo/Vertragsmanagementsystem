@{
    ViewBag.Title = "Import aus CSV Datei";
    Layout = "~/Views/Shared/_LayoutModal.cshtml";
}

<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
    <h4 class="modal-title" id="myModalLabel">Import aus CSV Datei</h4>
</div>

@using (Html.BeginForm())
{
    <div class="modal-body">
        <h2>Verträge importieren</h2>
        <label>Wählen Sie eine CSV Datei aus, die die zu importierenden Verträge enthält</label>

        <div class="form-horizontal">
            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
            <div class="form-group">
                <input id="selectFile" type="file" name="path" />
            </div>
        </div>
    </div>
    <div class="modal-footer">
        <button class="btn" data-dismiss="modal">Abbrechen</button>
        <button class="btn btn-primary" type="submit" onclick="retToParent(); ret=false;">Daten importieren</button>
    </div>
}
<script type="text/javascript">
    function retToParent() {
        //create the argument that will be returned to the parent page
        var oArg = new String();
        //var n = String.Format("{0}", Request.Form["customerName"]);
        oArg = String.Format("{0}", Request.Form["selectFile"]);
        //get the city's name

        $.post('@Url.Action("Import","Contract")',{path:selectFile});
    }
</script>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}






